import AppointmentModalBody from './AppointmentModalBody'
import AppointmentModalForm from './AppointmentModalForm'
import AppointmentModalRoot from './AppointmentModalRoot'

export const AppointmentModal = {
  Root: AppointmentModalRoot,
  Body: AppointmentModalBody,
  Form: AppointmentModalForm,
}
